import { NumberOnlyValidatorDirective } from './number-only-validator.directive';

describe('NumberOnlyValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberOnlyValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
