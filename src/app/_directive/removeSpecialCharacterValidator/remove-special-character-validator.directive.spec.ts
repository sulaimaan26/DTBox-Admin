import { RemoveSpecialCharacterValidatorDirective } from './remove-special-character-validator.directive';

describe('RemoveSpecialCharacterValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveSpecialCharacterValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
